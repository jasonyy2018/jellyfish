export default function OpenClawPage() {
  return (
    <main style={{ maxWidth: 1100, margin: "24px auto", fontFamily: "sans-serif" }}>
      <h2>OpenClaw</h2>
      <p>Use API endpoint `POST /api/install/openclaw` to bootstrap OpenClaw runtime on target nodes.</p>
    </main>
  );
}
