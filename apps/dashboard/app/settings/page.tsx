export default function SettingsPage() {
  return (
    <main style={{ maxWidth: 1100, margin: "24px auto", fontFamily: "sans-serif" }}>
      <h2>Settings</h2>
      <p>Configure JWT secret, Redis endpoint, and API keys in environment variables.</p>
    </main>
  );
}
